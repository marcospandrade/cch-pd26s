import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center'
  },
  cardFormulario: {
    textAlign: 'center',
    padding: 20,
    margin: 20
  },
  cardTitle: {
    fontSize: 20,
  },
  inputsForm: {
    marginTop: 20,
  }
});